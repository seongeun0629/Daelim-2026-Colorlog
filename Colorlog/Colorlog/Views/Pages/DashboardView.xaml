<UserControl x:Class="Colorlog.Views.Pages.DashboardView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="900" d:DesignWidth="1100"
      Background="Transparent"
      SnapsToDevicePixels="True"
      UseLayoutRounding="True"
      Loaded="UserControl_Loaded"
      SizeChanged="UserControl_SizeChanged">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                  Background="Transparent"
                  SnapsToDevicePixels="True" UseLayoutRounding="True">
        <Grid Background="Transparent" Margin="{StaticResource AppShellPagePadding}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="{StaticResource PageHeaderBottomMargin}">
                <TextBlock Text="{Binding UserDisplayName, StringFormat={}{0}님의 상태 요약}" Style="{StaticResource PageHeaderTitleStyle}"/>
                <TextBlock Text="{Binding LastDiagnosisAtText, StringFormat=최근 분석 {0}}" Style="{StaticResource PageHeaderSubtitleStyle}"/>
                <TextBlock Text="오늘의 톤/피부 상태를 빠르게 확인하고 바로 다음 액션으로 이어가세요." Style="{StaticResource PageHeaderHintStyle}"/>
            </StackPanel>

            <Grid x:Name="DashboardTopCardsGrid" Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <Border x:Name="DashboardColorSummaryCard" Grid.Row="0" Grid.Column="0" Style="{StaticResource DashboardCardStyle}">
                    <StackPanel>
                        <TextBlock Text="컬러 요약" Style="{StaticResource DashboardCardTitleStyle}"/>
                        <TextBlock Text="{Binding PersonalColorName, StringFormat=현재 퍼스널 컬러: {0}}" Margin="0,10,0,0" Style="{StaticResource DashboardAccentValueStyle}"/>
                        <TextBlock Text="베스트 컬러" Margin="0,16,0,8" Style="{StaticResource DashboardCardSubTextStyle}"/>

                        <ItemsControl ItemsSource="{Binding BestColors}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,0,8,8" Padding="10,6" Style="{StaticResource GlassInsetStyle}">
                                        <StackPanel Orientation="Horizontal">
                                            <Ellipse Width="16" Height="16" Fill="{Binding ToneBrush}" Stroke="{StaticResource SwatchStrokeBrush}" StrokeThickness="1"/>
                                            <TextBlock Text="{Binding Name}" Margin="8,0,0,0" VerticalAlignment="Center" FontSize="12" Foreground="{StaticResource TextPrimaryBrush}"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border x:Name="DashboardRecommendCard" Grid.Row="0" Grid.Column="1" Style="{StaticResource DashboardCardStyle}">
                    <StackPanel>
                        <TextBlock Text="최근 화장품 추천 요약" Style="{StaticResource DashboardCardTitleStyle}"/>
                        <TextBlock Text="내 톤과 피부 상태 기준 상위 추천 제품" Margin="0,8,0,16" Style="{StaticResource DashboardCardSubTextStyle}"/>

                        <ItemsControl ItemsSource="{Binding RecentRecommendations}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,0,0,8" Padding="12" Style="{StaticResource GlassInsetStyle}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding ProductName}" FontWeight="SemiBold" FontSize="14" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                <TextBlock Text="{Binding Summary}" Margin="0,4,0,0" Style="{StaticResource DashboardCardSubTextStyle}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Summary}"/>
                                            </StackPanel>
                                            <Border Grid.Column="1" VerticalAlignment="Center" Background="{StaticResource RoseSoftBackgroundBrush}" BorderBrush="{StaticResource RoseBadgeBorderBrush}" BorderThickness="1" CornerRadius="14" Padding="8,4">
                                                <TextBlock Text="{Binding Rating, StringFormat=★ {0}}" Foreground="{StaticResource RoseEmphasisTextBrush}" FontSize="12" FontWeight="SemiBold"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border x:Name="DashboardSkinMetricsCard" Grid.Row="1" Grid.ColumnSpan="2" Style="{StaticResource DashboardCardStyle}">
                    <StackPanel>
                        <TextBlock Text="피부 컨디션 지표" Style="{StaticResource DashboardCardTitleStyle}"/>
                        <TextBlock Text="오늘 촬영 기준 핵심 지수" Margin="0,8,0,16" Style="{StaticResource DashboardCardSubTextStyle}"/>

                        <ItemsControl ItemsSource="{Binding SkinMetrics}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,16">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="130"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="130"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" Text="{Binding Name}" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TitleTextBrush}"/>

                                        <ProgressBar Grid.Column="1" Minimum="0" Maximum="100" Value="{Binding Percent, Mode=OneWay}" VerticalAlignment="Center"
                                                     Style="{StaticResource DashboardProgressBarStyle}"/>

                                        <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Current, StringFormat={}{0}/100}" FontSize="12" Foreground="{StaticResource MutedTextBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                            <Border Style="{StaticResource DashboardMetricTagStyle}">
                                                <TextBlock Text="{Binding StatusLabel}" FontSize="12" Foreground="{Binding AccentBrush}" FontWeight="SemiBold"/>
                                            </Border>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Grid.Row="2" Style="{StaticResource DashboardCardStyle}">
                <Expander IsExpanded="False" Background="Transparent" BorderThickness="0">
                    <Expander.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="피부톤 진단 결과 (Beta)" FontWeight="SemiBold" FontSize="15" Foreground="{StaticResource TitleTextBrush}"/>
                            <TextBlock Text="  얼굴 부위별 평균 색상 인사이트" Style="{StaticResource DashboardCardSubTextStyle}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Expander.Header>
                    <StackPanel Margin="0,16,0,0">
                        <ItemsControl ItemsSource="{Binding FaceZoneTones}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,0,8,8" Padding="12,10" Style="{StaticResource GlassInsetStyle}">
                                        <StackPanel Orientation="Horizontal">
                                            <Rectangle Width="28" Height="28" RadiusX="8" RadiusY="8" Fill="{Binding ZoneBrush}" Stroke="{StaticResource SwatchStrokeBrush}" StrokeThickness="1"/>
                                            <StackPanel Margin="8,0,0,0">
                                                <TextBlock Text="{Binding ZoneName}" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TitleTextBrush}"/>
                                                <TextBlock Text="평균 톤 추출 완료" FontSize="11" Foreground="{StaticResource MutedTextBrush}"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Expander>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
